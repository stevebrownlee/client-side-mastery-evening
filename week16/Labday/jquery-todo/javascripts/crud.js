var FbAPI = (function(oldFirebase) {

  oldFirebase.getTodos = function() {
  };

  oldFirebase.addTodo = function() {
  };

  oldFirebase.deleteTodo = function() {
  };

  oldFirebase.editTodo = function() {
  };

  return oldFirebase;

})(FbAPI || {});
